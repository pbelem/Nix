sudo -i

lsblk

1 step:
cfdisk /dev/sda

2 step:
mkfs.etx4 -L nixos /dev/sda2
mkfs.fat -F 32 -n BOOT /dev/sda1

3 step:
mount /dev/sda2 /mnt
mount --mkdir /dev/sda1 /mnt/boot

4 step:
nixos-generate-config --root /mnt

5 step:
cd /tmp
git clone https://github.com/pbelem/nixos-belem.git
sudo nixos-install --flake /tmp/nixos-belem#nixos-belem

6 step:
nixos-install --root /mnt --flake /mnt/etc/nixos#Desktop-NixOS
sudo nixos-rebuild switch --flake /etc/nixos#Desktop-NixOS

7 step:
nixos-enter --root /mnt -c 'passwd belem'

8 step:
reboot

# must confirm device name with: lsblk
mount /dev/sdc1 /mnt/Ventoy
sudo nixos-install --flake /mnt/Ventoy/Nix/#nixos-belem

https://github.com/polirritmico/Breeze-Dark-Cursor